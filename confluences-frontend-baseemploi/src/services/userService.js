/**
 * Projet: Gestion des stagiaires
 * Auteur : Tim Allemann
 * Date : 16.09.2020
 * Description : Configurations des requêtes API pour les utilisateurs
 * Fichier : userService.js (Mis à jour pour Vue 3 / import.meta.env)
 **/

import axios from 'axios'

// CORRECTION: Remplacer process.env par import.meta.env
const API_URL = import.meta.env.VITE_API_URL
const CONTROLLER = '/api/Users'

export default {
  // Récupère tous les utilisateurs
  getUsers() {
    return axios.get(API_URL + CONTROLLER)
  }
}